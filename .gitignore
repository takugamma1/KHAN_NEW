.DS_Store
node_modules/
*.log

# scroll-sequence test frames (deployed via CLI, not tracked)
assets/seq-*.webp

# lossless original frames (360MB — deployed via CLI only, kept out of git)
assets/seq2o-*.webp
assets/seq2u-*.webp
assets/khan-seq-*.mp4
