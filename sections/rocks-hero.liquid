{% liquid
  assign bg_type = section.settings.media_type
  assign has_image = false
  assign has_video = false
  if bg_type == 'image' and section.settings.background_image != blank
    assign has_image = true
  endif
  if bg_type == 'video' and section.settings.background_video != blank
    assign has_video = true
  endif

  assign fetch_priority = 'auto'
  if section.index == 1
    assign fetch_priority = 'high'
  endif
%}

<div
  id="RocksHero-{{ section.id }}"
  class="rocks-hero color-{{ section.settings.color_scheme }}{% if section.settings.text_grain %} rocks-hero--grain{% endif %}"
  style="
    {% if section.settings.section_height == 'custom' %}
      --rocks-hero-min-height: {{ section.settings.section_height_custom }}svh;
    {% elsif section.settings.section_height == 'full-screen' %}
      --rocks-hero-min-height: 100svh;
    {% else %}
      --rocks-hero-min-height: var(--section-height-{{ section.settings.section_height }});
    {% endif %}
    --rocks-frame-fit: {{ section.settings.frame_fit }};
    --rocks-logo-width: {{ section.settings.logo_width }}px;
    --rocks-logo-width-mobile: {{ section.settings.logo_width_mobile }}px;
    --rocks-pulse-duration: {{ section.settings.pulse_speed }}s;
    --rocks-pulse-scale: {{ section.settings.pulse_amount | divided_by: 1000.0 | plus: 1 }};
    --rocks-pulse-min-opacity: {{ section.settings.pulse_opacity | divided_by: 100.0 }};
    --rocks-accent: {{ section.settings.accent_color }};
    --rocks-heading-size: {{ section.settings.heading_size }}px;
    --rocks-text-size: {{ section.settings.text_size }}px;
    --rocks-ast-size: {{ section.settings.asterisk_size }}px;
    --rocks-ast-gap: {{ section.settings.asterisk_gap }}px;
    --rocks-ast-inset: {{ section.settings.asterisk_inset }}px;
    --rocks-grain: url('{{ 'khan-grain.png' | asset_url }}');
    --rocks-sh-x: {{ section.settings.shadow_x }}px;
    --rocks-sh-y: {{ section.settings.shadow_y }}px;
    --rocks-sh-blur: {{ section.settings.shadow_blur }}px;
    --rocks-sh-color: {{ section.settings.shadow_color }};
    --usp-justify: {{ section.settings.usp_align }};
    --usp-bottom: {{ section.settings.usp_bottom }}px;
    --usp-icon: {{ section.settings.usp_icon_size }}px;
    --usp-title-size: {{ section.settings.usp_title_size }}px;
    --usp-text-size: {{ section.settings.usp_text_size }}px;
    --usp-divider: {{ section.settings.usp_divider_color }};
    --usp-card-width: {{ section.settings.usp_card_width }}px;
  "
  {% if request.visual_preview_mode %}
    data-shopify-visual-preview
  {% endif %}
>
  {%- comment -%} Layer 1: background media (image or video). Black when empty -> drop video here. {%- endcomment -%}
  <div class="rocks-hero__media" data-testid="rocks-hero-media">
    {%- if has_image -%}
      {{
        section.settings.background_image
        | image_url: width: 3840
        | image_tag:
          width: section.settings.background_image.width,
          widths: '832, 1200, 1600, 1920, 2560, 3840',
          height: section.settings.background_image.height,
          alt: section.settings.background_image.alt,
          class: 'rocks-hero__media-item',
          sizes: '100vw',
          fetchpriority: fetch_priority,
          data-testid: 'rocks-hero-image'
      }}
    {%- elsif has_video -%}
      {{
        section.settings.background_video
        | video_tag:
          poster: nil,
          autoplay: true,
          loop: true,
          controls: false,
          muted: true,
          class: 'rocks-hero__media-item',
          data-testid: 'rocks-hero-video'
      }}
    {%- endif -%}
  </div>

  {%- comment -%} Layer 2: optional tint overlay {%- endcomment -%}
  {%- if section.settings.toggle_overlay -%}
    {% render 'overlay', settings: section.settings, layer: 'var(--layer-flat)' %}
  {%- endif -%}

  {%- comment -%} Layer 3: the rocks frame image, sits on top of the media {%- endcomment -%}
  {%- if section.settings.frame_image != blank -%}
    <div class="rocks-hero__frame" data-testid="rocks-hero-frame">
      {{
        section.settings.frame_image
        | image_url: width: 3840
        | image_tag:
          width: section.settings.frame_image.width,
          widths: '832, 1200, 1600, 1920, 2560, 3840',
          height: section.settings.frame_image.height,
          alt: '',
          class: 'rocks-hero__frame-img',
          sizes: '100vw',
          fetchpriority: fetch_priority
      }}
    </div>
  {%- endif -%}

  {%- comment -%} Layer 4: centered logo, white + subtle pulse {%- endcomment -%}
  {%- if section.settings.show_logo -%}
  <div
    class="
      rocks-hero__logo
      {% if section.settings.logo_pulse %}rocks-hero__logo--pulse{% endif %}
      {% if section.settings.logo_white %}rocks-hero__logo--white{% endif %}
    "
    data-testid="rocks-hero-logo"
  >
    {%- if section.settings.logo != blank -%}
      {{
        section.settings.logo
        | image_url: width: 1200
        | image_tag:
          width: section.settings.logo.width,
          widths: '120, 240, 352, 480, 832, 1200',
          height: section.settings.logo.height,
          alt: shop.name,
          class: 'rocks-hero__logo-img',
          sizes: 'var(--rocks-logo-width)'
      }}
    {%- else -%}
      {{ 'white-logo.svg' | inline_asset_content }}
    {%- endif -%}
  </div>
  {%- endif -%}

  {%- comment -%} Layer 5: heading + paragraph overlay {%- endcomment -%}
  {%- if section.settings.heading != blank or section.settings.text != blank -%}
    <div
      class="
        rocks-hero__content
        rocks-hero__content--{{ section.settings.content_position }}
        {% if section.settings.use_nexa %}rocks-hero__content--nexa{% endif %}
        {% if section.settings.enable_shadow %}rocks-hero__content--shadow{% endif %}
      "
    >
      {%- if section.settings.heading != blank -%}
        <div class="rocks-hero__heading">{{ section.settings.heading }}</div>
      {%- endif -%}
      {%- if section.settings.text != blank -%}
        <div class="rocks-hero__text">{{ section.settings.text }}</div>
      {%- endif -%}
    </div>
  {%- endif -%}

  {%- comment -%} Layer 6: asterisks, bottom-right {%- endcomment -%}
  {%- if section.settings.show_asterisks -%}
    <div class="rocks-hero__asterisks">
      {%- for i in (1..section.settings.asterisk_count) -%}
        <img
          class="rocks-hero__asterisk"
          src="{{ 'asterisk.png' | asset_url }}"
          alt=""
          loading="lazy"
          width="258"
          height="320"
        >
      {%- endfor -%}
    </div>
  {%- endif -%}

  {%- comment -%} Layer 7: USP row — editable blocks, horizontal cards + vertical dividers {%- endcomment -%}
  {%- if section.blocks.size > 0 -%}
    <div class="rocks-hero__usps">
      {%- for block in section.blocks -%}
        <div class="rocks-hero__usp" {{ block.shopify_attributes }}>
          {%- if block.settings.icon != blank -%}
            <img class="rocks-hero__usp-icon" src="{{ block.settings.icon | image_url: width: 200 }}" alt="" width="96" height="96" loading="lazy">
          {%- endif -%}
          <span class="rocks-hero__usp-dash" aria-hidden="true"></span>
          {%- if block.settings.title != blank -%}<h3 class="rocks-hero__usp-title">{{ block.settings.title }}</h3>{%- endif -%}
          {%- if block.settings.text != blank -%}<div class="rocks-hero__usp-text">{{ block.settings.text }}</div>{%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  {%- endif -%}
</div>

{% stylesheet %}
  .rocks-hero {
    position: relative;
    min-height: var(--rocks-hero-min-height);
    overflow: hidden;
    display: grid;
    place-items: center;
    /* Solid black so an empty media slot reads as "video goes here" */
    background-color: #000;
  }

  .rocks-hero[data-shopify-visual-preview] {
    min-height: 600px;
  }

  .rocks-hero__media {
    position: absolute;
    inset: 0;
    z-index: var(--layer-base, 0);
  }

  .rocks-hero__media-item,
  .rocks-hero__media img,
  .rocks-hero__media video {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    display: block;
  }

  .rocks-hero__frame {
    position: absolute;
    inset: 0;
    z-index: var(--layer-raised, 2);
    pointer-events: none;
  }

  .rocks-hero__frame-img {
    width: 100%;
    height: 100%;
    object-fit: var(--rocks-frame-fit, cover);
    object-position: center center;
    display: block;
  }

  .rocks-hero__logo {
    position: relative;
    z-index: var(--layer-heightened, 4);
    width: var(--rocks-logo-width, 200px);
    max-width: 80vw;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    will-change: transform, opacity;
  }

  .rocks-hero__logo img,
  .rocks-hero__logo svg {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Force any logo (raster or svg) to render white */
  .rocks-hero__logo--white img,
  .rocks-hero__logo--white svg {
    filter: brightness(0) invert(1);
  }

  .rocks-hero__logo--white svg path {
    fill: #fff;
  }

  @media screen and (max-width: 749px) {
    .rocks-hero__logo {
      width: var(--rocks-logo-width-mobile, 140px);
    }
  }

  /* Subtle pulse. Disabled when the visitor prefers reduced motion. */
  @media (prefers-reduced-motion: no-preference) {
    .rocks-hero__logo--pulse {
      animation: rocks-logo-pulse var(--rocks-pulse-duration, 3s) ease-in-out infinite;
    }
  }

  @keyframes rocks-logo-pulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 1;
    }

    50% {
      transform: scale(var(--rocks-pulse-scale, 1.035));
      opacity: var(--rocks-pulse-min-opacity, 0.9);
    }
  }

  /* --- Content overlay: heading + paragraph --- */
  .rocks-hero__content {
    position: absolute;
    z-index: var(--layer-heightened, 4);
    max-width: min(92%, 780px);
    padding: clamp(20px, 5vw, 64px);
    display: flex;
    flex-direction: column;
    pointer-events: none;
    text-transform: uppercase;
  }

  .rocks-hero__content--nexa {
    font-family: 'Nexa', var(--font-body--family, sans-serif);
  }

  .rocks-hero__content a {
    pointer-events: auto;
  }

  .rocks-hero__content--top-left {
    inset: 0 auto auto 0;
    align-items: flex-start;
    text-align: left;
  }

  .rocks-hero__content--top-right {
    inset: 0 0 auto auto;
    align-items: flex-end;
    text-align: right;
  }

  .rocks-hero__content--center-left {
    inset: 50% auto auto 0;
    transform: translateY(-50%);
    align-items: flex-start;
    text-align: left;
  }

  .rocks-hero__content--center {
    inset: 50% auto auto 50%;
    transform: translate(-50%, -50%);
    align-items: center;
    text-align: center;
  }

  .rocks-hero__content--bottom-left {
    inset: auto auto 0 0;
    align-items: flex-start;
    text-align: left;
  }

  .rocks-hero__content--bottom-right {
    inset: auto 0 0 auto;
    align-items: flex-end;
    text-align: right;
  }

  .rocks-hero__heading {
    margin: 0;
    font-weight: 900;
    font-size: var(--rocks-heading-size, 80px);
    line-height: 0.95;
    color: #fff;
  }

  .rocks-hero__text {
    margin: 0.6em 0 0;
    font-family: ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace;
    font-weight: 400;
    font-size: var(--rocks-text-size, 20px);
    line-height: 1.5;
    letter-spacing: 0.02em;
    color: #fff;
  }

  /* Bold words in the rich-text fields render in the red accent */
  .rocks-hero__heading :is(strong, b),
  .rocks-hero__text :is(strong, b) {
    color: var(--rocks-accent, #d8232a);
    font-weight: inherit;
  }

  .rocks-hero__heading p,
  .rocks-hero__text p {
    margin: 0;
  }

  .rocks-hero__content--shadow .rocks-hero__heading,
  .rocks-hero__content--shadow .rocks-hero__text {
    /* directional drop (configurable) + tight edge + soft ambient halo
       so the text stays legible over a busy background */
    text-shadow:
      var(--rocks-sh-x, 3px) var(--rocks-sh-y, 6px) var(--rocks-sh-blur, 8px) var(--rocks-sh-color, rgba(0, 0, 0, 0.55)),
      0 1px 2px rgba(0, 0, 0, 0.65),
      0 0 22px rgba(0, 0, 0, 0.32);
  }

  /* Grain on the content only — same treatment as the Our Story section */
  .rocks-hero--grain :is(.rocks-hero__heading, .rocks-hero__text, .rocks-hero__asterisk, .rocks-hero__usp-title, .rocks-hero__usp-text, .rocks-hero__usp-icon) {
    -webkit-mask-image: var(--rocks-grain);
    mask-image: var(--rocks-grain);
    -webkit-mask-size: 170px 170px;
    mask-size: 170px 170px;
    -webkit-mask-repeat: repeat;
    mask-repeat: repeat;
  }

  /* --- Asterisks, bottom-right --- */
  .rocks-hero__asterisks {
    position: absolute;
    z-index: var(--layer-heightened, 4);
    inset: auto var(--rocks-ast-inset, 32px) var(--rocks-ast-inset, 32px) auto;
    display: flex;
    align-items: center;
    gap: var(--rocks-ast-gap, 16px);
    color: var(--rocks-accent, #d8232a);
    pointer-events: none;
  }

  .rocks-hero__asterisk {
    display: block;
    width: var(--rocks-ast-size, 44px);
    height: auto;
    flex: 0 0 auto;
  }

  @media screen and (max-width: 749px) {
    .rocks-hero__heading {
      font-size: calc(var(--rocks-heading-size, 80px) * 0.5);
    }

    .rocks-hero__text {
      font-size: calc(var(--rocks-text-size, 20px) * 0.85);
    }

    .rocks-hero__asterisk {
      width: calc(var(--rocks-ast-size, 44px) * 0.7);
    }
  }
  /* --- USP row (editable blocks): horizontal cards + vertical dividers --- */
  .rocks-hero__usps {
    position: absolute;
    left: 0;
    right: 0;
    bottom: var(--usp-bottom, 48px);
    z-index: var(--layer-heightened, 4);
    display: flex;
    flex-wrap: wrap;
    justify-content: var(--usp-justify, center);
    align-items: stretch;
    padding-inline: clamp(20px, 5vw, 64px);
    pointer-events: none;
  }

  .rocks-hero__usp {
    flex: 0 1 var(--usp-card-width, 230px);
    max-width: var(--usp-card-width, 230px);
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 0 28px;
    border-left: 1px solid var(--usp-divider, rgb(255 255 255 / 0.28));
  }

  .rocks-hero__usps > .rocks-hero__usp:first-child {
    border-left: 0;
  }

  .rocks-hero__usp-icon {
    width: var(--usp-icon, 64px);
    height: var(--usp-icon, 64px);
    object-fit: contain;
    object-position: center;
    display: block;
    margin-bottom: 14px;
  }

  .rocks-hero__usp-dash {
    width: 26px;
    height: 3px;
    margin-bottom: 14px;
    background: var(--rocks-accent, #d8232a);
    display: block;
  }

  .rocks-hero__usp-title {
    margin: 0 0 8px;
    font-family: 'Nexa', var(--font-heading--family, sans-serif);
    font-weight: 900;
    font-size: var(--usp-title-size, 18px);
    line-height: 1.05;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--rocks-accent, #d8232a);
  }

  .rocks-hero__usp-text {
    margin: 0;
    font-family: ui-monospace, "SFMono-Regular", Menlo, Consolas, monospace;
    font-size: var(--usp-text-size, 14px);
    line-height: 1.5;
    color: #fff;
  }

  .rocks-hero__usp-text p { margin: 0; }

  @media screen and (max-width: 749px) {
    .rocks-hero__usp {
      flex: 1 1 45%;
      max-width: 50%;
      padding: 0 10px;
      margin-bottom: 14px;
    }
    .rocks-hero__usps > .rocks-hero__usp:nth-child(odd) { border-left: 0; }
    .rocks-hero__usp-title { font-size: calc(var(--usp-title-size, 18px) * 0.85); }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Rocks hero",
  "tag": "section",
  "class": "rocks-hero-wrapper section-wrapper",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Background media"
    },
    {
      "type": "paragraph",
      "content": "Image or video sitting behind the frame. Leave empty for a solid black backdrop (a video can be dropped in later)."
    },
    {
      "type": "select",
      "id": "media_type",
      "label": "Type",
      "options": [
        { "value": "image", "label": "Image" },
        { "value": "video", "label": "Video" }
      ],
      "default": "image"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background image",
      "visible_if": "{{ section.settings.media_type == 'image' }}"
    },
    {
      "type": "video",
      "id": "background_video",
      "label": "Background video",
      "visible_if": "{{ section.settings.media_type == 'video' }}"
    },
    {
      "type": "header",
      "content": "Frame"
    },
    {
      "type": "paragraph",
      "content": "The frame image sits on top of the background media. Use a PNG with a transparent center (your rocks frame)."
    },
    {
      "type": "image_picker",
      "id": "frame_image",
      "label": "Frame image"
    },
    {
      "type": "select",
      "id": "frame_fit",
      "label": "Frame fit",
      "options": [
        { "value": "cover", "label": "Cover (fill, may crop)" },
        { "value": "fill", "label": "Stretch to edges" },
        { "value": "contain", "label": "Contain (no crop)" }
      ],
      "default": "cover"
    },
    {
      "type": "header",
      "content": "Logo"
    },
    {
      "type": "checkbox",
      "id": "show_logo",
      "label": "Show logo",
      "default": true
    },
    {
      "type": "paragraph",
      "content": "Centered in the section. Leave empty to use the bundled white logo. Turn off \"Show logo\" to remove it entirely; turn off \"Pulse the logo\" to keep it static."
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Logo image"
    },
    {
      "type": "checkbox",
      "id": "logo_white",
      "label": "Force logo white",
      "default": true
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "Logo width (desktop)",
      "min": 60,
      "max": 800,
      "step": 10,
      "unit": "px",
      "default": 220
    },
    {
      "type": "range",
      "id": "logo_width_mobile",
      "label": "Logo width (mobile)",
      "min": 40,
      "max": 500,
      "step": 10,
      "unit": "px",
      "default": 140
    },
    {
      "type": "header",
      "content": "Pulse"
    },
    {
      "type": "checkbox",
      "id": "logo_pulse",
      "label": "Pulse the logo",
      "default": true
    },
    {
      "type": "range",
      "id": "pulse_amount",
      "label": "Pulse size",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "‰",
      "default": 35,
      "info": "How much the logo grows at the peak of the pulse. Kept subtle by default.",
      "visible_if": "{{ section.settings.logo_pulse }}"
    },
    {
      "type": "range",
      "id": "pulse_opacity",
      "label": "Pulse dimming",
      "min": 50,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 90,
      "info": "Lowest opacity reached during the pulse.",
      "visible_if": "{{ section.settings.logo_pulse }}"
    },
    {
      "type": "range",
      "id": "pulse_speed",
      "label": "Pulse speed",
      "min": 1,
      "max": 8,
      "step": 1,
      "unit": "s",
      "default": 3,
      "info": "Seconds per pulse cycle.",
      "visible_if": "{{ section.settings.logo_pulse }}"
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "paragraph",
      "content": "Bold text becomes the red accent. All text shows in capitals. Uses the Nexa font."
    },
    {
      "type": "richtext",
      "id": "heading",
      "label": "Heading"
    },
    {
      "type": "richtext",
      "id": "text",
      "label": "Paragraph"
    },
    {
      "type": "select",
      "id": "content_position",
      "label": "Position",
      "options": [
        { "value": "top-left", "label": "Top left" },
        { "value": "top-right", "label": "Top right" },
        { "value": "center-left", "label": "Center left" },
        { "value": "center", "label": "Center" },
        { "value": "bottom-left", "label": "Bottom left" },
        { "value": "bottom-right", "label": "Bottom right" }
      ],
      "default": "center-left"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent (red) color",
      "default": "#d8232a"
    },
    {
      "type": "checkbox",
      "id": "use_nexa",
      "label": "Use Nexa font",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_shadow",
      "label": "Text shadow",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "text_grain",
      "label": "Grain on text & asterisks",
      "default": true
    },
    {
      "type": "range",
      "id": "shadow_x",
      "label": "Shadow offset X",
      "min": 0,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 3,
      "visible_if": "{{ section.settings.enable_shadow }}"
    },
    {
      "type": "range",
      "id": "shadow_y",
      "label": "Shadow offset Y",
      "min": 0,
      "max": 30,
      "step": 1,
      "unit": "px",
      "default": 6,
      "visible_if": "{{ section.settings.enable_shadow }}"
    },
    {
      "type": "range",
      "id": "shadow_blur",
      "label": "Shadow blur",
      "min": 0,
      "max": 40,
      "step": 1,
      "unit": "px",
      "default": 8,
      "visible_if": "{{ section.settings.enable_shadow }}"
    },
    {
      "type": "color",
      "id": "shadow_color",
      "label": "Shadow color",
      "alpha": true,
      "default": "#0000008c",
      "visible_if": "{{ section.settings.enable_shadow }}"
    },
    {
      "type": "range",
      "id": "heading_size",
      "label": "Heading size",
      "min": 24,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "text_size",
      "label": "Paragraph size",
      "min": 12,
      "max": 48,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "header",
      "content": "Asterisks"
    },
    {
      "type": "checkbox",
      "id": "show_asterisks",
      "label": "Show asterisks",
      "default": true
    },
    {
      "type": "range",
      "id": "asterisk_count",
      "label": "Number of asterisks",
      "min": 1,
      "max": 8,
      "step": 1,
      "default": 4
    },
    {
      "type": "range",
      "id": "asterisk_size",
      "label": "Asterisk size",
      "min": 16,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 44
    },
    {
      "type": "range",
      "id": "asterisk_gap",
      "label": "Gap between asterisks",
      "min": 0,
      "max": 60,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "asterisk_inset",
      "label": "Distance from corner",
      "min": 0,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "section_height",
      "label": "Height",
      "options": [
        { "value": "small", "label": "Small" },
        { "value": "medium", "label": "Medium" },
        { "value": "large", "label": "Large" },
        { "value": "full-screen", "label": "Full screen" },
        { "value": "custom", "label": "Custom" }
      ],
      "default": "full-screen"
    },
    {
      "type": "range",
      "id": "section_height_custom",
      "label": "Custom height",
      "min": 20,
      "max": 100,
      "step": 1,
      "unit": "%",
      "default": 100,
      "visible_if": "{{ section.settings.section_height == 'custom' }}"
    },
    {
      "type": "header",
      "content": "Appearance"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "toggle_overlay",
      "label": "Tint media",
      "default": false
    },
    {
      "type": "color",
      "id": "overlay_color",
      "label": "Tint color",
      "alpha": true,
      "default": "#00000040",
      "visible_if": "{{ section.settings.toggle_overlay }}"
    },
    {
      "type": "select",
      "id": "overlay_style",
      "label": "Tint style",
      "options": [
        { "value": "solid", "label": "Solid" },
        { "value": "gradient", "label": "Gradient" }
      ],
      "default": "solid",
      "visible_if": "{{ section.settings.toggle_overlay }}"
    },
    {
      "type": "select",
      "id": "gradient_direction",
      "label": "Gradient direction",
      "options": [
        { "value": "to top", "label": "Up" },
        { "value": "to bottom", "label": "Down" }
      ],
      "default": "to top",
      "visible_if": "{{ section.settings.toggle_overlay and section.settings.overlay_style == 'gradient' }}"
    },
    {
      "type": "header",
      "content": "USPs (bottom row)"
    },
    {
      "type": "paragraph",
      "content": "Add USP blocks below. Each shows an icon, a dash, a title and a description, separated by vertical lines."
    },
    {
      "type": "select",
      "id": "usp_align",
      "label": "Row alignment",
      "options": [
        { "value": "flex-start", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "flex-end", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "range",
      "id": "usp_bottom",
      "label": "Distance from bottom",
      "min": 0,
      "max": 200,
      "step": 4,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "usp_card_width",
      "label": "Card width",
      "min": 140,
      "max": 320,
      "step": 2,
      "unit": "px",
      "default": 230
    },
    {
      "type": "range",
      "id": "usp_icon_size",
      "label": "USP icon size",
      "min": 24,
      "max": 120,
      "step": 2,
      "unit": "px",
      "default": 64
    },
    {
      "type": "range",
      "id": "usp_title_size",
      "label": "USP title size",
      "min": 12,
      "max": 32,
      "step": 1,
      "unit": "px",
      "default": 18
    },
    {
      "type": "range",
      "id": "usp_text_size",
      "label": "USP text size",
      "min": 11,
      "max": 24,
      "step": 1,
      "unit": "px",
      "default": 14
    },
    {
      "type": "color",
      "id": "usp_divider_color",
      "label": "Divider line color",
      "alpha": true,
      "default": "#ffffff47"
    }
  ],
  "blocks": [
    {
      "type": "usp",
      "name": "USP",
      "settings": [
        { "type": "image_picker", "id": "icon", "label": "Icon" },
        { "type": "text", "id": "title", "label": "Title", "default": "Science backed" },
        { "type": "richtext", "id": "text", "label": "Description", "default": "<p>Research. Evidence. Real results.</p>" }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Rocks hero",
      "category": "t:categories.banners"
    }
  ]
}
{% endschema %}
